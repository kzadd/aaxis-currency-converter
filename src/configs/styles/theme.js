import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    brand: {
      bgInput: '#EFEFEF',
      darkGray: '#808080',
      darkGrayText: '#3c3c3c',
      lightGray: '#f6f6f6',
      primary: '#1f2261',
      primaryHover: '#26278D',
      white: '#FFFFFF'
    }
  }
})

export default theme
